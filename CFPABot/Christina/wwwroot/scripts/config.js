// Global configuration for Christina app
// Single source of truth for API base URL
(function () {
    // Change this in one place for all pages
    window.API_BASE_URL = 'https://cfpa.cyan.cafe/api/Christina';
})();
